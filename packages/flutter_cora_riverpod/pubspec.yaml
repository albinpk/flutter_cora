name: flutter_cora_riverpod

description: "A simple Flutter package for separating UI and business logic using a view-controller pattern."

version: 0.0.1-dev.2

# homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

topics:
  - mvc
  - riverpod
  - view-controller
  - architecture
  - clean-code
