name: FlutterCora

repository: https://github.com/albinpk/flutter_cora

packages:
  - packages/*
  - bricks/flutter_cora/

sdkPath: .fvm/flutter_sdk

command:
  version:
    hooks:
      # Update the version in readme
      preCommit: ./scripts/preVersion.sh && ./scripts/h.sh
      post: ./scripts/hook.sh
